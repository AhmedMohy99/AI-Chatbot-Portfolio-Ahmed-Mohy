/*
 * CloseBot source configuration.
 *
 * IMPORTANT: CloseBot generates a unique Sales Intelligence Pixel script for
 * each connected source. Copy that exact script from:
 * CloseBot -> Sources -> your HubSpot source -> Setup
 * and assign it below as a JavaScript string.
 *
 * Do not put a CloseBot API key here. The website only needs the public
 * browser pixel/widget script.
 */
window.CLOSEBOT_PIXEL_SCRIPT = '';
